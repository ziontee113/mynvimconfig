function myFunc(arg) {
  if (LHS === RHS) {
    //TODO:;
  }

  if (LHS === RHS) {
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(RHS);
    }
  }
}
