function myFunc(arg) {
  if (LHS === RHS) {
    //TODO:;
  }

  if (LHS === RHS) {
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }
    if (LHS === RHS) {
      console.log(LHS);
    }

    if (LHS === RHS) {
      console.log(LHS);
    } else if (LHS === RHS) {
      console.log(LHS);
    } else {
      console.log(LHS);
    }

    if (LHS === RHS) {
      console.log(RHS);
    }
  }
}
